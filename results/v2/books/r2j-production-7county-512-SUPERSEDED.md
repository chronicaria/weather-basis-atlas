# Superseded representative book run

The JSON, CSV, and memo bytes in `r2j-production-7county-512/` are retained
for review, but must not be used for a decision or comparison. Its producer
carried the May 31 station AR state directly to July 1, omitting the required
coherent June process bridge. Platform will replace the input with calendar-
aligned 2,000-path public and 10,000-path offline artifacts. Recompile with
`compile_sample_books`; do not modify or infer a correction from these outputs.
