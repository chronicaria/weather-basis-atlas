# GHCN golden excerpts

These three complete station-month excerpts are copied from the frozen
`ghcnd_snap2026-09-02` files under `data/raw/ghcnd/`.  They retain the raw
tenths-Celsius `TMAX` and `TMIN` values and attribute triplets needed by the
dependency-free calculator in `tests/golden/independent_hdd.py`.

