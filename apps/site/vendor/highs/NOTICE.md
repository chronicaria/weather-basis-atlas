# HiGHS browser solver

This directory vendors `highs` 1.15.2 (highs-js), an MIT-licensed WebAssembly
build of HiGHS. The original MIT license is retained in `LICENSE`; exact package
integrity and asset SHA-256 values are in `MANIFEST.json`. The Worker loads only
these local assets and never contacts a solver service.
